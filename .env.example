# SkillSure API (leave empty to use built-in mock data for listings)
# VITE_API_BASE_URL=https://api.example.com

# Set to "true" to require a stored session (after login) for /admin, /employer, and /candidate app routes
# VITE_ENFORCE_AUTH=true
